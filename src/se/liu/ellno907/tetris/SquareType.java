package se.liu.ellno907.tetris;

/**
 * A enum to describe the different possible tetrominos
 * EMPTY represents a square that's not occupied by a tetromino
 * OUTSIDE represents the outer borders of the board
 */
public enum SquareType
{
    EMPTY, I, O, T, S, Z, J, L,OUTSIDE
}